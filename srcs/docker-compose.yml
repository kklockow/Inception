# version: '3.9'

services:
  mysql:
    build:
      context: ./requirements/mariadb
      dockerfile: Dockerfile
    volumes:
      - db_volume:/var/lib/mysql
    env_file: "environment.env"
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "--silent"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - db_network

  wordpress:
    build:
      context: ./requirements/wordpress
      dockerfile: Dockerfile
    volumes:
      - wp_volume:/var/www/html
    depends_on:
      - mysql
    env_file: "environment.env"
    networks:
      - wp_network
      - db_network
    restart: always

  nginx:
    build:
      context: ./requirements/nginx
      dockerfile: Dockerfile
    volumes:
      - wp_volume:/var/www/html
    depends_on:
      - wordpress
    ports:
      - "443:443"
    networks:
      - wp_network
    restart: always

volumes:
  db_volume:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: /Users/kklockow/home/kklockow/data/db_volume
  wp_volume:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: /Users/kklockow/home/kklockow/data/wp_volume

networks:
  wp_network:
  db_network: